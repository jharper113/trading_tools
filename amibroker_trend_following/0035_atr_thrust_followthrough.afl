// ATR Thrust Follow-Through

SetOption("InitialEquity", Param("Initial Equity", 100000, 10000, 1000000, 10000));
SetOption("MaxOpenPositions", Param("Max Open Positions", 10, 1, 50, 1));
SetOption("AllowSameBarExit", False);
SetTradeDelays(1, 1, 1, 1);
SetPositionSize(Param("Position % Equity", 6, 1, 100, 1), spsPercentOfEquity);

atrLen = Param("ATR Length", 14, 5, 50, 1);
thrustATR = Param("Thrust ATR", 1.2, 0.5, 3, 0.1);
maxHold = Param("Max Hold Bars", 2, 1, 5, 1);
trendLen = Param("Trend EMA", 20, 5, 100, 5);

trendEMA = EMA(Close, trendLen);
body = Close - Open;
longThrust = body > thrustATR * ATR(atrLen) AND Close > trendEMA;
shortThrust = body < -thrustATR * ATR(atrLen) AND Close < trendEMA;

Buy = Ref(longThrust, -1) AND Close > Ref(High, -1);
Sell = Close < Ref(Low, -1);
Short = Ref(shortThrust, -1) AND Close < Ref(Low, -1);
Cover = Close > Ref(High, -1);

ApplyStop(stopTypeNBar, stopModeBars, maxHold, True);
ApplyStop(stopTypeLoss, stopModePoint, 1.2 * ATR(atrLen), True);

Buy = ExRem(Buy, Sell);
Sell = ExRem(Sell, Buy);
Short = ExRem(Short, Cover);
Cover = ExRem(Cover, Short);

Plot(Close, "Close", colorDefault, styleCandle);
Plot(trendEMA, "Trend EMA", colorBlue, styleLine);
