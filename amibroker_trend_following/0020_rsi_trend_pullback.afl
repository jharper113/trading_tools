// RSI Pullback In Trend

SetOption("InitialEquity", Param("Initial Equity", 100000, 10000, 1000000, 10000));
SetOption("MaxOpenPositions", Param("Max Open Positions", 10, 1, 50, 1));
SetOption("AllowSameBarExit", False);
SetTradeDelays(1, 1, 1, 1);
SetPositionSize(Param("Position % Equity", 8, 1, 100, 1), spsPercentOfEquity);

trendLen = Param("Trend EMA", 200, 50, 400, 10);
pullbackLevel = Param("Pullback RSI", 40, 20, 50, 1);
triggerLevel = Param("Trigger RSI", 50, 45, 60, 1);
rsiLen = Param("RSI Length", 14, 5, 40, 1);
atrLen = Param("ATR Length", 20, 5, 100, 1);

trendEMA = EMA(Close, trendLen);
r = RSI(rsiLen);

recentLongPullback = BarsSince(r < pullbackLevel) < 10;
recentShortPullback = BarsSince(r > 100 - pullbackLevel) < 10;

Buy = Close > trendEMA AND recentLongPullback AND Cross(r, triggerLevel);
Sell = Close < trendEMA OR Cross(45, r);
Short = Close < trendEMA AND recentShortPullback AND Cross(100 - triggerLevel, r);
Cover = Close > trendEMA OR Cross(r, 55);

ApplyStop(stopTypeTrailing, stopModePoint, 3 * ATR(atrLen), True, True);

Buy = ExRem(Buy, Sell);
Sell = ExRem(Sell, Buy);
Short = ExRem(Short, Cover);
Cover = ExRem(Cover, Short);

Plot(Close, "Close", colorDefault, styleCandle);
Plot(trendEMA, "Trend EMA", colorBlue, styleLine);
