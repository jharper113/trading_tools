// Volatility-Adjusted Momentum

SetOption("InitialEquity", Param("Initial Equity", 100000, 10000, 1000000, 10000));
SetOption("MaxOpenPositions", Param("Max Open Positions", 10, 1, 50, 1));
SetOption("AllowSameBarExit", False);
SetTradeDelays(1, 1, 1, 1);
SetPositionSize(Param("Position % Equity", 8, 1, 100, 1), spsPercentOfEquity);

momLen = Param("Momentum Lookback", 126, 42, 300, 14);
atrLen = Param("ATR Length", 20, 5, 100, 1);
smoothLen = Param("Score Smooth", 10, 1, 50, 1);
exitThreshold = Param("Exit Threshold", 0, -2, 2, 0.1);

volPct = 100 * ATR(atrLen) / Close;
score = EMA(ROC(Close, momLen) / Max(volPct, 0.01), smoothLen);

Buy = Cross(score, 1);
Sell = Cross(exitThreshold, score);
Short = Cross(-1, score);
Cover = Cross(score, -exitThreshold);

ApplyStop(stopTypeTrailing, stopModePoint, 4 * ATR(atrLen), True, True);

Buy = ExRem(Buy, Sell);
Sell = ExRem(Sell, Buy);
Short = ExRem(Short, Cover);
Cover = ExRem(Cover, Short);

Plot(Close, "Close", colorDefault, styleCandle);
Plot(score, "Vol-Adjusted Momentum", colorBlue, styleOwnScale);
